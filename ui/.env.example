# URL or path to the graph JSON data file
# In development, this defaults to /graph.json
# In production, point to your API endpoint or static file
VITE_GRAPH_DATA_URL=/graph.json

# Comma-separated list of allowed hosts for the Vite dev server
# VITE_ALLOWED_HOSTS=monitoring.zae.life,codebrain.zae.life
